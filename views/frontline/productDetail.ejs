<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= product.name %> - Mira's ST3S</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="frontline-body detail-page-active">

    <div class="product-detail-page">
        <div class="product-detail-image-header">
            <img src="<%= product.imageUrl || 'https://via.placeholder.com/300' %>" alt="<%= product.name %>">
            <a href="/" class="btn-close-detail">
                <i class="bi bi-x-lg"></i>
            </a>
        </div>

        <div class="product-detail-body">
            <div>
                <span class="badge badge-category"><%= product.category %></span>
                <h2 class="my-2" id="product-name"><%= product.name %></h2>
                <h1 class="fw-bold text-primary mb-4" id="product-price">₱<%= product.price.toFixed(2) %></h1>

                <p class="text-muted fw-bold">Quantity</p>
                <div class="d-flex align-items-center justify-content-between my-3">
                    <button class="btn btn-secondary btn-quantity" id="quantity-minus">-</button>
                    <input type="number" id="quantity-input" class="form-control text-center mx-3 fs-3 fw-bold border-0" value="1" min="1" readonly>
                    <button class="btn btn-secondary btn-quantity btn-primary" id="quantity-plus">+</button>
                </div>
                
                <hr>

                <div class="d-flex justify-content-between align-items-center">
                    <p class="text-muted mb-0">Total Price</p>
                    <h3 class="fw-bold mb-0" id="total-price">₱<%= product.price.toFixed(2) %></h3>
                </div>
            </div>

            <div class="mt-4">
                <button type="button" class="btn btn-primary w-100 btn-lg" id="add-to-cart-btn" data-product-id="<%= product._id %>">
                    <i class="bi bi-bag-check-fill me-2"></i>Add to Cart
                </button>
            </div>
        </div>
    </div>

    <%- include('../partials/frontlineBottomNav') %>
    <%- include('../partials/frontlineFooter') %>
</body>
</html>