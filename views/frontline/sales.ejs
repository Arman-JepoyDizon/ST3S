<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Today's Sales - Mira's ST3S</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="frontline-body" style="padding-top: 0;">

    <header class="cart-header">
        <a href="/" class="back-button"><i class="bi bi-arrow-left"></i></a>
        <div>
            <h1 class="h4 mb-0 fw-bold">Today's Sales</h1>
            <p class="text-white-50 mb-0"><%= new Date().toLocaleDateString('en-US', { month: 'long', day: 'numeric', year: 'numeric' }) %></p>
        </div>
    </header>

    <main class="container-fluid sales-main-content">
        <div class="card sales-summary-card mb-4">
            <div class="card-body">
                <div class="row">
                    <div class="col-12 mb-3">
                        <p class="text-muted mb-0">Total Sales</p>
                        <h2 class="fw-bold text-white">₱<%= totalSales.toFixed(2) %></h2>
                    </div>
                    <div class="col-4">
                        <p class="text-muted mb-0 small">Orders</p>
                        <h4 class="fw-bold"><%= totalOrders %></h4>
                    </div>
                    <div class="col-4">
                        <p class="text-muted mb-0 small">Completed</p>
                        <h4 class="fw-bold">0</h4> </div>
                    <div class="col-4">
                        <p class="text-muted mb-0 small">Average</p>
                        <h4 class="fw-bold">₱<%= (totalOrders > 0 ? (totalSales / totalOrders).toFixed(2) : '0.00') %></h4>
                    </div>
                </div>
            </div>
        </div>

        <% if (transactions && transactions.length > 0) { %>
            <% transactions.forEach(transaction => { %>
                <div class="card transaction-card mb-3">
                    <div class="card-body">
                        <div class="d-flex justify-content-between align-items-start">
                            <div>
                                <h6 class="fw-bold">ORD-<%= transaction._id.toString().slice(-6).toUpperCase() %></h6>
                                <small class="text-muted">
                                    <i class="bi bi-clock"></i> 
                                    <%= new Date(transaction.createdAt).toLocaleTimeString('en-US', { hour: '2-digit', minute: '2-digit' }) %>
                                </small>
                            </div>
                            <span class="badge badge-status-pending">Pending</span>
                        </div>
                        <hr>
                        <div class="d-flex justify-content-between align-items-center">
                            <div>
                                <p class="text-muted mb-0"><%= transaction.items.length %> <%= transaction.items.length > 1 ? 'items' : 'item' %></p>
                            </div>
                            <div>
                                <small class="text-muted">Total</small>
                                <p class="fw-bold text-primary fs-5 mb-0">₱<%= transaction.totalAmount.toFixed(2) %></p>
                            </div>
                        </div>
                    </div>
                </div>
            <% }) %>
        <% } else { %>
            <div class="text-center text-muted mt-5">
                <div class="empty-cart-icon mb-3">
                    <i class="bi bi-receipt"></i>
                </div>
                <p>No sales recorded yet for today.</p>
            </div>
        <% } %>

    </main>

    <%- include('../partials/frontlineBottomNav') %>
    <%- include('../partials/frontlineFooter') %>
</body>
</html>