<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mira's Menu - ST3S</title>
    
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="manifest" href="/manifest.json">
    <link rel="apple-touch-icon" href="/icons/icon-192x192.png">
    <meta name="theme-color" content="#FF0066">
    
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="frontline-body">

    <header class="frontline-header">
        <div class="header-top-row">
            <h1 class="h4 mb-0 fw-bold">✨ Mira's Menu</h1>
            <div class="cart-icon-wrapper">
                <div class="cart-link-wrapper">
                    <a href="/cart" class="text-white">
                        <i class="bi bi-cart3 fs-4"></i>
                        <span class="badge rounded-pill bg-white text-dark cart-count">0</span>
                    </a>
                </div>
                <a href="#" class="text-white" data-bs-toggle="modal" data-bs-target="#logoutConfirmModal">
                    <i class="bi bi-person-circle fs-4"></i>
                </a>
            </div>
        </div>
   
        <p class="header-greeting">What would you like to order?</p>

        <div class="search-bar-wrapper">
            <i class="bi bi-search search-bar-icon"></i>
            <input type="search" class="form-control search-bar" id="searchMenu" placeholder="Search delicious food...">
        </div>
    </header>

    <div class="modal fade" id="logoutConfirmModal" tabindex="-1" aria-labelledby="logoutModalLabel" aria-hidden="true">
        <div class="modal-dialog modal-dialog-centered">
            <div class="modal-content">
                <div class="modal-body text-center p-4">
                    <h5 class="mb-3" id="logoutModalLabel">Do you wish to logout?</h5>
                    <p class="text-muted small">You will be logged out automatically.</p>
                    
                    <div class="progress" role="progressbar" aria-label="Logout countdown" aria-valuenow="100" aria-valuemin="0" aria-valuemax="100" style="height: 10px">
                        <div class="progress-bar bg-primary" id="logout-progress-bar" style="width: 100%"></div>
                    </div>

                    <div class="d-grid gap-2 mt-4">
                        <button type="button" class="btn btn-outline-secondary" data-bs-dismiss="modal">Cancel</button>
                        <a href="/logout" class="btn btn-primary" id="confirm-logout-btn">Logout Now</a>
                    </div>
                </div>
            </div>
        </div>
    </div>